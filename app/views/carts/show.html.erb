<h1>蒐藏庫</h1>

  <table class="table table-hover">
    <thead>
      <tr>
        <td>廠商名稱</td>
        <td>地址</td>
      </tr>
    </thead>
    <tbody>
      <% current_cart.items.each do |item| %>
        <tr>
          <td><%= item.food.name %></td>

        </tr>
      <% end %>
    </tbody>
  </table>

  <%= link_to '繼續挑選', foods_path, class: "btn btn-default" %>
