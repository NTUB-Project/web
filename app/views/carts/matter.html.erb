</br>
</br>
</br>
<div class="content">
<div class="sub_menu">
  <div class="sub_title">
    <div><button class="click_log" id="Selfbtn" onclick="openCategory('Self'),changeColor('Selfbtn')">自行輸入</button></div>
    <div><button class="click_log" id="Formbtn" onclick="openCategory('Form'),changeColor('Formbtn')">表格</button></div>
  </div>
</div>

<div id="Self" class="categor">
  <div class="container">
    <h2>輸入信件內容</h2>
    <%= simple_form_for matter_cart_path(@matter) , :url => {:action => :matter_send } do |f| %>
      <div class="form-inputs">
        <%= f.input :mattertext %>
      </div>
      <div class="form-actions">
        <%= f.button :submit,"寄出" %>
      </div>
    <% end %>
  </div>
</div>


<div id="Form" class="categor" style="display:none">
  <div class="container">
    <%= simple_form_for matter_cart_path(@matter_form)  , :url => {:action => :matter_form_send } do |f| %>
      <div class="form-inputs">
        <%= f.input :email, label: "聯絡信箱：" %>
        <%= f.input :school, label: "學校/單位：" %>
        <%= f.input :date, label: "活動日期：", as: :datetime, start_year: Date.today.year,
                                                              end_year: Date.today.year + 5, discard_day: true %>
        <%= f.input :people, label: "預估活動人數：" %>
        <%= f.input :memo, label: "備註" %>
      </div>
      <div class="form-actions">
        <%= f.button :submit,"寄出" %>
      </div>
    <% end %>
  </div>
</div>

</div>

<script>
function openCategory(categoryName) {
  var i;
  var x = document.getElementsByClassName("categor");
  for (i = 0; i < x.length; i++) {
     x[i].style.display = "none";
  }
  document.getElementById(categoryName).style.display = "block";
}
function changeColor(btnName) {
  var x = document.getElementsByClassName("click_log");
  for (i = 0; i < x.length; i++) {
     x[i].style.color = "black";
  }
  document.getElementById(btnName).style.color = "TEAL";
}
</script>
